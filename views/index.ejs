<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Grocery Admin Panel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
  <style>
    :root {
      --primary-green: #3BB77E;
      --light-green: #f8fff9;
      --shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
    }
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      font-family: 'Inter', sans-serif;
    }
    .sidebar {
      position: fixed;
      left: 0;
      top: 0;
      height: 100vh;
      width: 280px;
      background: white;
      box-shadow: var(--shadow);
      padding: 20px;
      z-index: 1000;
      border-radius: 0 20px 20px 0;
    }
    .sidebar .logo {
      text-align: center;
      margin-bottom: 30px;
      padding: 20px 0;
      border-bottom: 1px solid #eee;
    }
    .sidebar .nav-link {
      color: #666;
      padding: 12px 20px;
      margin: 5px 0;
      border-radius: 12px;
      transition: all 0.3s;
      text-decoration: none;
      display: flex;
      align-items: center;
    }
    .sidebar .nav-link i {
      margin-right: 10px;
      width: 20px;
    }
    .sidebar .nav-link:hover,
    .sidebar .nav-link.active {
      background: var(--primary-green);
      color: white;
      box-shadow: var(--shadow);
    }
    .sidebar .logout {
      position: absolute;
      bottom: 20px;
      left: 20px;
      right: 20px;
    }
    .top-header {
      margin-left: 280px;
      background: white;
      padding: 15px 30px;
      box-shadow: var(--shadow);
      border-radius: 0 0 20px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .search-box {
      position: relative;
      width: 300px;
    }
    .search-box input {
      width: 100%;
      padding: 10px 40px 10px 15px;
      border: 1px solid #ddd;
      border-radius: 25px;
      outline: none;
    }
    .search-box i {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: #999;
    }
    .header-icons {
      display: flex;
      align-items: center;
      gap: 20px;
    }
    .header-icons i {
      font-size: 20px;
      color: #666;
      cursor: pointer;
    }
    .profile-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: var(--primary-green);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
    }
    .main-content {
      margin-left: 280px;
      margin-top: 20px;
      padding: 30px;
    }
    .card {
      background: white;
      border: none;
      border-radius: 16px;
      box-shadow: var(--shadow);
    }
    .btn-primary {
      background: var(--primary-green);
      border: none;
      border-radius: 12px;
      padding: 10px 20px;
    }
    .btn-primary:hover {
      background: #2a8f5f;
    }
    .section {
      display: none;
    }
    .section.active {
      display: block;
    }
    .badge {
      border-radius: 20px;
    }
    .table th {
      background: var(--light-green);
      border: none;
      color: #333;
      font-weight: 600;
    }
    .table td {
      border: none;
      vertical-align: middle;
    }
  </style>
</head>
<body>
  <!-- Sidebar -->
  <div class="sidebar">
    <div class="logo">
      <h4 style="color: var(--primary-green); margin: 0;">🛒 Grocery Admin</h4>
    </div>
    <nav class="nav flex-column">
      <a href="#" class="nav-link active" data-section="dashboard">
        <i class="fas fa-tachometer-alt"></i> Dashboard
      </a>
      <a href="#" class="nav-link" data-section="categories">
        <i class="fas fa-tags"></i> Categories
      </a>
      <a href="#" class="nav-link" data-section="subcategories">
        <i class="fas fa-list"></i> Subcategories
      </a>
      <a href="#" class="nav-link" data-section="products">
        <i class="fas fa-box"></i> Products
      </a>
      <a href="#" class="nav-link" data-section="banners">
        <i class="fas fa-image"></i> Banners
      </a>
      <a href="#" class="nav-link" data-section="orders">
        <i class="fas fa-shopping-cart"></i> Orders
      </a>
      <a href="#" class="nav-link" data-section="users">
        <i class="fas fa-users"></i> Users
      </a>
      <a href="#" class="nav-link" data-section="analytics">
        <i class="fas fa-chart-bar"></i> Analytics
      </a>
      <a href="#" class="nav-link" data-section="settings">
        <i class="fas fa-cog"></i> Settings
      </a>
    </nav>
    <a href="#" class="nav-link logout">
      <i class="fas fa-sign-out-alt"></i> Logout
    </a>
  </div>

  <!-- Top Header -->
  <div class="top-header">
    <div class="search-box">
      <input type="text" placeholder="Search your items…">
      <i class="fas fa-search"></i>
    </div>
    <div class="header-icons">
      <i class="fas fa-bell"></i>
      <i class="fas fa-envelope"></i>
      <i class="fas fa-cog"></i>
      <div class="profile-avatar">
        <i class="fas fa-user"></i>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="main-content">
    <!-- Dashboard Section -->
    <div id="dashboard" class="section active">
      <%- include('dashboard') %>
    </div>

    <!-- Categories Section -->
    <div id="categories" class="section">
      <%- include('categories') %>
    </div>

    <!-- Subcategories Section -->
    <div id="subcategories" class="section">
      <%- include('subcategories') %>
    </div>

    <!-- Products Section -->
    <div id="products" class="section">
      <%- include('products') %>
    </div>

    <!-- Banners Section -->
    <div id="banners" class="section">
      <%- include('banners') %>
    </div>

    <!-- Orders Section -->
    <div id="orders" class="section">
      <%- include('orders') %>
    </div>

    <!-- Users Section -->
    <div id="users" class="section">
      <%- include('users') %>
    </div>

    <!-- Analytics Section -->
    <div id="analytics" class="section">
      <h4>Analytics</h4>
      <p>Analytics content here.</p>
    </div>

    <!-- Settings Section -->
    <div id="settings" class="section">
      <h4>Settings</h4>
      <p>Settings content here.</p>
    </div>
  </div>

  <script>
    // Navigation functionality
    document.querySelectorAll('.nav-link').forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault();

        // Update active nav link
        document.querySelectorAll('.nav-link').forEach(l => l.classList.remove('active'));
        this.classList.add('active');

        // Show corresponding section (guard if section missing)
        const sectionId = this.getAttribute('data-section');
        document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
        const target = document.getElementById(sectionId);
        if (target) target.classList.add('active');
      });
    });

    // Initialize event listeners when DOM is loaded
    document.addEventListener('DOMContentLoaded', function() {
      // Only add listeners if elements exist (for add-single and add-bulk sections)
      const subcategorySelect = document.getElementById('subcategorySelect');
      if (subcategorySelect) {
        subcategorySelect.addEventListener('change', function() {
          const isNew = !this.value;
          const subcategoryNameInput = document.getElementById('subcategoryNameInput');
          const subcategoryImageInput = document.getElementById('subcategoryImageInput');
          if (subcategoryNameInput) subcategoryNameInput.style.display = isNew ? 'block' : 'none';
          if (subcategoryImageInput) subcategoryImageInput.style.display = isNew ? 'block' : 'none';
        });
      }
    });

    function addProductRow() {
      const area = document.getElementById('productsArea');
      if (!area) return;
      const row = document.createElement('div');
      row.className = 'row g-2 mb-2 product-row';
      row.innerHTML = `
        <div class="col-md-3"><input type="text" class="form-control" placeholder="Product Name" required></div>
        <div class="col-md-3"><input type="number" class="form-control" placeholder="Product Price" required></div>
        <div class="col-md-4"><input type="file" name="productImages" class="form-control" accept="image/*" required></div>
        <div class="col-md-2 d-grid"><button type="button" class="btn btn-danger" onclick="removeProductRow(this)">Remove</button></div>
      `;
      area.appendChild(row);
    }
    function removeProductRow(btn) {
      btn.closest('.product-row').remove();
    }
    function prepareBulkProducts() {
      const productsInput = document.getElementById('productsInput');
      if (!productsInput) return true;
      const rows = document.querySelectorAll('.product-row');
      const products = [];
      for (const row of rows) {
        const inputs = row.querySelectorAll('input');
        const name = inputs[0] ? inputs[0].value : '';
        const priceRaw = inputs[1] ? inputs[1].value : '';
        const price = priceRaw === '' ? 0 : Number(priceRaw) || 0;
        products.push({
          name,
          price
        });
      }
      productsInput.value = JSON.stringify(products);
      return true;
    }
    async function updateSubcategoryOptions() {
      const categoryEl = document.getElementById('categorySelect');
      const subcategorySelect = document.getElementById('subcategorySelect');

      // Guard: if either element is missing, don't proceed
      if (!categoryEl || !subcategorySelect) return;

      const categoryId = categoryEl.value;

      subcategorySelect.innerHTML = '<option value="">Create New Subcategory</option>';

      if (!categoryId) return;

      try {
        const res = await fetch(`/category/${categoryId}`);
        if (!res.ok) {
          console.warn('Failed to fetch subcategories', res.status);
          return;
        }
        const data = await res.json();

        console.log("Fetched Subcategories:", data);

        if (data && data.subcategories && Array.isArray(data.subcategories)) {
          data.subcategories.forEach(sub => {
            const option = document.createElement('option');
            // prefer _id if provided by backend
            option.value = sub.id || sub._id || '';
            option.textContent = sub.name || sub.title || 'Unnamed';
            subcategorySelect.appendChild(option);
          });
        }
      } catch (error) {
        console.log("Error fetching subcategories:", error);
      }
    }

  </script>
</body>
</html>
